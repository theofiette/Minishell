# Minishell
A minimal Unix shell written in C, implementing pipelines, I/O redirections, logical operators, subshell execution, environment variable expansion, heredocs, wildcard (*) expansion, and robust Unix signal handling. Provides interactive command history and exit status propagation.
